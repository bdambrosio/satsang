# Core ML and Training Dependencies
torch>=2.0.0
transformers>=4.56.2
peft>=0.7.0
datasets>=3.0.0
accelerate>=1.4.0
trl>=0.27.0
safetensors>=0.4.3

# Optional: Experiment Tracking
wandb>=0.15.0

# Optional: API-based Critic Models (for aliveness_critic.py)
openai>=1.0.0
anthropic>=0.18.0
httpx>=0.25.0

# Standard dependencies (usually included with above, but explicit for clarity)
numpy>=1.17.0
packaging>20.0
